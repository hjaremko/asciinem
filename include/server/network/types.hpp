#ifndef ASCIINEM_TYPES_HPP
#define ASCIINEM_TYPES_HPP

#include <string>

namespace asciinem::server::network::types
{

using msg = std::string;
using id = std::string;
using ip = std::string;
using port = unsigned short;

} // namespace asciinem::server::network::types

#endif // ASCIINEM_TYPES_HPP
